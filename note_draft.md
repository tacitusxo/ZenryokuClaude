# スモビジの経理をClaudeで自動化する──「仕訳して」で終わるワークフロー

## はじめに

売上が立つたびにStripeから入金があり、クレカの明細は毎月複数枚降ってきて、海外送金はWiseで動いて、証券口座では株を売ったり買ったり。それを全部仕訳して、会計ソフトに取り込んで、税理士にレビューしてもらう──これを一人でやり続けると、毎月かなりの時間が消えます。

去年から、Claude Codeにこの仕事をほぼ丸投げするようにしました。今では月次の仕訳作業は「仕訳して」と打つだけで終わります。

この仕組みをOSSとして公開したので、その紹介と設計の話を書きます。

こういったネタを定期的に発信しているので、Xもフォローしてもらえると嬉しいです。
https://x.com/tacitusxo

---

## どんな仕組みか

一言でいうと、**銀行・クレカの明細CSVをClaudeに読み込ませて、AIが仕訳し、会計ソフト用のCSVを吐き出す**仕組みです。

対応している口座の例：クレジットカード、銀行、証券、外貨口座、個人立替、旅費。

出力は**全力会計**のインポート形式（22列CSV）です。会計ソフトを変えたい場合はエクスポート部分だけ書き換えれば使えます。

---

## なぜ全力会計を選んだか

freeeやマネーフォワードを使っていた時期もあったのですが、今は全力会計に落ち着いています。一番の理由は**コストが安い**こと。

freeeやマネーフォワードがプランに応じて月数千円かかるのに対し、全力会計は月額980円（税抜）。1年間の無料期間もあり、固定費を削りたいスタートアップ初期には助かります。

機能面でも、仕訳の入力・インポート・帳票出力と必要なものはひととおり揃っています。余計なUI機能が少ない分、CSVインポートの仕様がシンプルで、スクリプトから叩きやすいのも利点でした。

「全力会計 インポート形式」で検索すると公式マニュアルが出てきます。22列のCSVを決められた順番で並べてShift-JISで保存するだけ。この仕様がはっきりしているおかげで、今回のスクリプトを書くのが楽でした。

---

## 実際の操作

操作は2パターンです。

### パターン1：仕訳する

各口座の明細CSVをダウンロードしてフォルダに置き、Claude Codeに話しかけるだけです。

```
クレジットカード/  ← カード明細.csv を置く
銀行/             ← 銀行明細.csv を置く
証券/             ← 証券口座の明細.csv を置く
個人立替/         ← 立替経費の明細.csv を置く
旅費/             ← 旅費・交通費の明細.csv を置く
```

```
仕訳して
```

これだけです。Claudeが以下を自動でやってくれます：

1. 各フォルダのCSVを検出して取り込む（重複は自動スキップ）
2. 未分類の取引一覧を確認
3. 勘定科目を判定してDBに書き込む
4. 判断が難しい取引（初見の業者など）はチャットで確認してくる
5. 全件分類完了後、全力会計用CSVをエクスポート

エクスポートされたCSVを全力会計にインポートすれば完了です。この手順だけは手動ですが、数クリックで終わります。

### パターン2：領収書を整理する

```
領収書を整理して
```

電子帳簿保存法では、電子取引の証憑を検索できる形で保存する義務があります。このパターンはその対応です。

`領収書/` フォルダに放り込んだPDFを、ClaudeがPDFを直接読んで日付・業者名・金額を抽出し、`YYYY-MM-DD_業者名_金額.pdf` にリネームして月別フォルダに整理してくれます。スキャンPDFでも動きます。

---

## 技術的な設計（エンジニア向け）

### SQLite中間DBを使う理由

最初はCSVをそのまま処理しようとしたのですが、以下の問題が出ました：

- 「このファイルは今月もう取り込んだっけ？」という重複問題
- 仕訳した結果をどこに保存するか
- 月をまたいで集計したいとき

SQLiteを間に挟むことで、`import_log` テーブルでファイル単位の重複チェックができ、`transactions` テーブルと `journals` テーブルに分けて管理できるようになりました。Claudeがチャットで仕訳した結果はすべてDBに入るので、「先月この業者はどう処理したっけ」という参照も一発です。

### Claudeへの指示の渡し方

`CLAUDE.md` と `.claude/skills/keiri-shiwake/skill.md` の2段構成にしています。

`CLAUDE.md` はフォルダ構造とワークフローの説明だけ。仕訳ルールの細かい話（税区分の判定・伝票No命名規則・按分ルールなど）は `skill.md` に分離しています。これで `CLAUDE.md` を肥大化させずに済みます。

`/keiri-shiwake` スキルを呼ぶことで、Claudeは以下を参照します：

- 全力会計の22列フォーマット定義
- 伝票No命名規則（`[口座コード2桁][YYMMDD][連番2桁]`）
- 税区分の判定ルール（課対仕入10%、非課税売上、対象外など）
- 按分ルール（家賃・電気代の事業按分）
- 口座別の典型仕訳パターン

### 為替レートの自動取得

Wiseで外貨決済した取引は、[frankfurter.app](https://www.frankfurter.app/) APIから取引日のTTMレート近似値を自動取得してDBに保存しています。外部APIなので失敗することもありますが、その場合は処理を止めずに `None` で継続します。

---

## OSS化にあたって除いたもの

リポジトリには以下は含まれていません：

- `accounting.db`（実際の取引データ）
- 各口座の明細CSV
- 領収書PDF
- 過去の仕訳履歴
- 税理士レビューメモ

`.gitignore` で実データを完全に除外しているので、クローンしても空のフォルダ構造とスクリプトだけが手に入ります。自分のデータは自分のローカルにしか残らない設計です。

コード内に書いていた代表者のカタカナ氏名と役員報酬金額は、設定定数として外出しにしました：

```python
# accounting.py 冒頭で設定
REPRESENTATIVE_KATAKANA = ["代表者セイ", "代表者メイ"]  # 自社代表者名に変更
SALARY_OFFSET_AMOUNT = 1_200_000  # 役員報酬との相殺金額
```

---

## 使ってみたい方へ

GitHubに置いてあります。

https://github.com/tacitusxo/ZenryokuClaude

サンプルとして参照してください。

必要なものはPython 3.11+とClaude Codeだけです。会計ソフトは全力会計向けに出力していますが、エクスポート関数を書き換えればfreee・マネーフォワードなど他のソフトにも対応できます。

フォークして自社向けにカスタマイズして使ってください。issueやPRも歓迎です。

---

## おわりに

「AIに経理をやらせる」というと大げさに聞こえるかもしれませんが、実態はシンプルです。明細CSVを読んで勘定科目を当てはめる作業は、パターンの多い判断仕事です。Claudeはこれが得意で、しかも「この取引、交際費と消耗品費どっちですか？」と確認しながら進めてくれるので、最終的な判断は人間が持てます。

毎月の経理時間が大幅に減った分、税理士とのやり取りや資金計画の検討に時間を使えるようになりました。

ここで紹介したのはあくまで私の一例です。仕訳業務はカード会社・会計ソフト・業種によってやり方が変わるので、このnoteとGitHubのサンプルプロジェクトを参考に、自分の状況に合ったシステムを作ってみてください。
