## Claudeへの指示

### 仕訳のClaudeへの指示

ユーザーが「仕訳して」と言ったら、Claudeは以下を自動で行う：

1. `python3 accounting.py import` を実行して新しいCSVを取り込む
2. `python3 accounting.py status` で未分類取引を確認する
3. 未分類取引を勘定科目に分類してDBの `journals` テーブルに書き込む
4. 曖昧な取引はチャットでユーザーに確認してから書き込む
5. 全件分類完了後に `python3 accounting.py export` を実行する

### 領収書整理のClaudeへの指示

ユーザーが「領収書を整理して」と言ったら、Claudeは以下を自動で行う：

1. `領収書/` 直下にある `.pdf` ファイルを列挙する
2. 各PDFをReadツールで直接読み取り、日付・ベンダー名・金額を抽出する
3. ファイル名を `YYYY-MM-DD_ベンダー名_金額.pdf` 形式に決定する
4. `領収書/YYYY/MM/` サブフォルダを作成して移動する（Bashで `mkdir -p` + `mv`）
5. 処理できなかったPDFはユーザーに確認する

## 仕訳対象口座・フォルダ構造

### 仕訳対象

| フォルダ名       | 内容                                   |
| ---------------- | -------------------------------------- |
| クレジットカード | クレジットカード                       |
| 銀行             | 銀行口座                               |
| 証券             | 証券口座                               |
| 個人立替         | 立替経費（個人クレカ・現金による建替） |
| 旅費             | 旅費・交通費の精算                     |

### その他フォルダ

| フォルダ名 | 内容                                                                     |
| ---------- | ------------------------------------------------------------------------ |
| マニュアル | 全力会計のマニュアル                                                     |
| 勘定科目   | 全力会計に存在しない仕訳科目の定義・管理                                 |
| 開始残高   | 全力会計移行時にMFクラウド等の他会計ソフトから持ち込んだ開始残高ファイル |
| 領収書     | 領収書（YYYY/MM/ サブフォルダで月別管理）                                |

### フォルダ構造ルール

**仕訳対象口座フォルダ**（クレジットカード, 銀行, 証券, 個人立替, 旅費）は以下の構成：

```
{フォルダ名}/
  {ダウンロードしたCSV}.csv  ← 銀行・カード会社からダウンロードしたCSVを直接置く
  出力ファイル/              ← 変換済み全力会計形式CSVが出力される
  処理済み/                  ← 処理完了後に入力・出力CSVをまとめて移動する
```

**例外**: マニュアル, 領収書, 勘定科目, 開始残高 はこの構造に従わない（用途が異なるため）

### 領収書フォルダの構造

領収書PDFは取引月ごとにサブフォルダで管理する：

```
領収書/
  YYYY/
    MM/
      YYYY-MM-DD_内容_金額.pdf
```

- ファイル名形式: `YYYY-MM-DD_ベンダー名_JPY金額.pdf` または `YYYY-MM-DD_ベンダー名_USD金額.pdf`
- 新しい領収書PDFを追加したら「領収書を整理して」とClaudeに依頼する
- テキストPDFもスキャンPDFもClaudeが直接読み取ってリネーム・移動する（スクリプト不要）

## 仕訳ルール・全力会計フォーマット

仕訳作業のルール（勘定科目一覧・税区分・伝票No規則・按分ルール・典型パターン等）は `/keiri-shiwake` スキルを参照すること。
